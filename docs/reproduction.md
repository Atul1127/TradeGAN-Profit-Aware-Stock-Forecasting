# Reproduction

1. Install dependencies from `requirements.txt`.
2. Download the required historical prices into `data/` using `scripts/download_data.py`.
3. Use an experiment configuration from `configs/`.
4. Run training with the scripts in `scripts/`.
5. Evaluate forecasts and trading performance and store generated artifacts under `results/`.

Historical outputs retained in this repository are separated from source code.
